// Skeleton per PLAN.md Phase 1 — real content (pending-applications queue, moderation,
// reports) lands in later phases. Role-gated to "admin" in apps/web/src/proxy.ts.
export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Admin Dashboard</h1>
      <p className="mt-2 text-sm opacity-70">Coming in later phases.</p>
    </div>
  );
}
