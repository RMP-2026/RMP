// Skeleton per PLAN.md Phase 1 — real content (vehicle CRUD, bookings, analytics) lands
// in later phases. Company-membership authorization happens per-request in packages/api's
// "company" tRPC procedure builder (checks company_staff), not here or in middleware.
export default function CompanyDashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Company Dashboard</h1>
      <p className="mt-2 text-sm opacity-70">Coming in later phases.</p>
    </div>
  );
}
