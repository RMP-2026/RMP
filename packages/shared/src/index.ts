// Placeholder — shared types/constants/pricing/state-machine logic land here across
// later phases (see PLAN.md), e.g. the shared booking-status "blocking status" set
// referenced by both the availability constraint and the search query.
export {};
